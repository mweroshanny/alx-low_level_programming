#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int l_case;

	for (l_case = 'a'; l_case <= 'z'; l_case++)
	{
		putchar(l_case);

	}
		putchar('\n');
	return (0);
}
